name: Build Image

on:
    pull_request:
    push:
        branches:
            - main

jobs:
    build-image:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - run: echo "Placeholder"